# Install with:  pip install -r requirements.txt
#
# This file lists DIRECT dependencies (pinned, verified compatible).
# Deployment installs requirements.lock instead — a full pip freeze of a
# working environment — because re-resolving the crewai/chromadb graph
# fresh caused ResolutionImpossible build failures. To upgrade: bump here,
# install locally, verify, then regenerate the lock:
#   pip freeze | grep -vi fakeredis > requirements.lock

crewai[google-genai]==1.14.6   # Agents, Tasks, Crews, BaseTool + native Gemini client
pydantic==2.12.5               # Data contracts
python-dotenv==1.2.2           # Loads GEMINI_API_KEY / CACHE_REDIS_URL from .env
fastapi==0.136.3               # The web layer (briefing_engine/api.py)
uvicorn[standard]==0.49.0      # ASGI server:  uvicorn briefing_engine.api:app
redis==8.0.0                   # Durable cache tier (free Upstash; optional via CACHE_REDIS_URL)
